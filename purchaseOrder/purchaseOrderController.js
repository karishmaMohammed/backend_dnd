const { poPositionModel } = require('../models/poPosition'); 

async function createDNDPo(req, res){
    let responseData;
    try {
        
    } catch (error) {
        
    }
}

module.exports = {
    createDNDPo
}